<Page x:Class="MebelShop.Director.EmployeesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Background="{DynamicResource MaterialDesignPaper}"
      TextElement.Foreground="{DynamicResource MaterialDesignBody}"
      Title="EmployeesPage">

    <materialDesign:DialogHost Identifier="RootDialog" x:Name="EmployeesDialogHost">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Фильтры -->
            <StackPanel Orientation="Horizontal" Margin="0 0 0 15">
                <TextBox VerticalAlignment="Center" x:Name="IdFilterBox" Width="80" materialDesign:HintAssist.Hint="ID"/>
                <TextBox Margin="10 0" VerticalAlignment="Center" x:Name="EmailFilterBox" Width="150" materialDesign:HintAssist.Hint="Email"/>
                <ComboBox VerticalAlignment="Center" x:Name="RoleFilterBox" Width="150" materialDesign:HintAssist.Hint="Роль">
                    <ComboBoxItem Content="Все сотрудники" IsSelected="True"/>
                    <ComboBoxItem Content="Администратор"/>
                    <ComboBoxItem Content="Директор"/>
                    <ComboBoxItem Content="Менеджер"/>
                    <ComboBoxItem Content="Покупатель"/>
                </ComboBox>
                <TextBox Margin="10 0" VerticalAlignment="Center" x:Name="FirstNameFilterBox" Width="140" materialDesign:HintAssist.Hint="Имя"/>
                <TextBox VerticalAlignment="Center" x:Name="SurnameFilterBox" Width="140" materialDesign:HintAssist.Hint="Фамилия"/>

                <Button Content="↻ Сбросить" Click="Reset_Click" Margin="10 0 0 0"/>
                <Button Content="＋ Добавить" Click="AddUser_Click" Margin="10 0 0 0"/>
            </StackPanel>

            <!-- Таблица -->
            <DataGrid x:Name="UsersGrid"
                      MouseDoubleClick="UsersGrid_MouseDoubleClick"
                      MouseRightButtonDown="UsersDataGrid_MouseRightButtonDown"
                      Grid.Row="1"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      Margin="0"
                      FontSize="14">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Изменить пользователя" Click="ChangeUser_Click"/>
                        <MenuItem Header="Удалить пользователя" Click="DeleteUser_Click"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding IdUser}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"/>
                    <DataGridTextColumn Header="Роль" Binding="{Binding Role}" Width="2*"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="2*"/>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding Surname}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <materialDesign:DialogHost.DialogContent>
            <Grid x:Name="EditDialog" Margin="20" Width="400">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Редактирование сотрудника"
                           x:Name="DialogTitle" 
                           FontSize="18" FontWeight="Bold" Margin="0 0 0 15"/>

                <StackPanel Grid.Row="1">
                    <TextBox x:Name="EditEmailBox" materialDesign:HintAssist.Hint="Электронная почта" />
                    <ComboBox x:Name="EditRoleBox" materialDesign:HintAssist.Hint="Роль" SelectedIndex="0" Margin="0 10">
                        <ComboBoxItem Content="Администратор"/>
                        <ComboBoxItem Content="Директор"/>
                        <ComboBoxItem Content="Менеджер"/>
                        <ComboBoxItem Content="Покупатель"/>
                    </ComboBox>
                    <TextBox x:Name="EditFirstNameBox" materialDesign:HintAssist.Hint="Имя" />
                    <TextBox x:Name="EditSurnameBox" materialDesign:HintAssist.Hint="Фамилия" Margin="0 10"/>
                    <PasswordBox x:Name="EditPasswordBox" materialDesign:HintAssist.Hint="Новый пароль" />
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 15 0 0">
                    <Button Content="Отмена"
                            Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
                    <Button Content="Удалить"
                            Margin="5 0"
                            Click="DeleteUser_Click"/>
                    <Button Content="Сохранить"
                            Click="SaveUser_Click"/>
                </StackPanel>
            </Grid>
        </materialDesign:DialogHost.DialogContent>
    </materialDesign:DialogHost>
</Page>
