<Window x:Class="MebelShop.Customer.CustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Title="MebelShop" MinWidth="1024" MinHeight="720"
        Closing="Window_Closing">

    <DockPanel>
        <!-- Top navigation bar -->
        <Border DockPanel.Dock="Top">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#546E7A" Offset="0"/>
                    <GradientStop Color="#37474F" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#000" Opacity="0.20" BlurRadius="10" ShadowDepth="2" Direction="270"/>
            </Border.Effect>

            <Grid Height="64">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,30,0">
                    <TextBlock Text="🪑" FontSize="24" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="MebelShop" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    <Border Width="3" Background="#FFC107" Margin="14,8,0,8" CornerRadius="2"/>
                </StackPanel>

                <!-- Navigation buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="📦  Каталог"
                            Style="{StaticResource NavFlatButton}"
                            Click="CatalogButton_Click"/>
                    <Button Content="🛒  Корзина"
                            Style="{StaticResource NavFlatButton}"
                            Click="CartButton_Click"/>
                    <Button Content="👤  Профиль"
                            Style="{StaticResource NavFlatButton}"
                            Click="ProfileButton_Click"/>
                </StackPanel>

                <!-- Theme toggle -->
                <Button Grid.Column="2"
                        Content="{DynamicResource ThemeIcon}"
                        Width="44" Height="44"
                        Margin="10,0"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Foreground="White"
                        Click="ToggleTheme_Click"
                        ToolTip="Сменить тему"/>
            </Grid>
        </Border>

        <!-- Amber accent line -->
        <Border DockPanel.Dock="Top" Height="3">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#FFC107" Offset="0"/>
                    <GradientStop Color="#FF8F00" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" />
    </DockPanel>
</Window>
