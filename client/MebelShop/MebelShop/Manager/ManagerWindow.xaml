<Window x:Class="MebelShop.Manager.ManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Title="MebelShop: Менеджер" MinWidth="1024" MinHeight="720"
        Closing="Window_Closing">
    <DockPanel>
        <Border DockPanel.Dock="Top">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#546E7A" Offset="0"/>
                    <GradientStop Color="#37474F" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#000" Opacity="0.20" BlurRadius="10" ShadowDepth="2" Direction="270"/>
            </Border.Effect>
            <Grid Height="64">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,30,0">
                    <TextBlock Text="🪑" FontSize="22" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="MebelShop" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    <Border Width="2" Background="#FFC107" Margin="12,8,0,8" CornerRadius="1"/>
                    <TextBlock Text=" Менеджер" FontSize="13" Foreground="#90A4AE" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="📋  Заказы" Style="{StaticResource NavFlatButton}" Click="OrdersButton_Click"/>
                    <Button Content="💬  Отзывы" Style="{StaticResource NavFlatButton}" Click="ReviewsButton_Click"/>
                </StackPanel>

                <Button Grid.Column="2" Content="Выйти" Height="36" Padding="16,0"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Foreground="White" BorderBrush="#90A4AE"
                        Margin="0,0,8,0"
                        Click="LogoutButton_Click"/>
                <Button Grid.Column="3" Content="{DynamicResource ThemeIcon}" Width="44" Height="44"
                        Margin="0,0,10,0"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Foreground="White"
                        Click="ToggleTheme_Click"/>
            </Grid>
        </Border>
        <Border DockPanel.Dock="Top" Height="3">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#FFC107" Offset="0"/>
                    <GradientStop Color="#FF8F00" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>
        <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" />
    </DockPanel>
</Window>
