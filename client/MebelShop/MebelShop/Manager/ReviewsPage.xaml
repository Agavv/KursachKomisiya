<Page x:Class="MebelShop.Manager.ReviewsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Background="{DynamicResource MaterialDesignPaper}"
      TextElement.Foreground="{DynamicResource MaterialDesignBody}"
      Title="Отзывы">

    <materialDesign:DialogHost x:Name="ReviewDialogHost">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Top">
                <!-- Фильтр по рейтингу -->
                <ComboBox x:Name="RatingFilterComboBox"
                          Width="140"
                          Margin="0,0,10,0"
                          SelectionChanged="FilterChanged"
                          materialDesign:HintAssist.Hint="Рейтинг">
                    <ComboBoxItem Content="Все" IsSelected="True"/>
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="4"/>
                    <ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="1"/>
                </ComboBox>

                <!-- Фильтр по дате -->
                <ComboBox x:Name="DateFilterComboBox"
                          Width="160"
                          Margin="0,0,10,0"
                          SelectionChanged="FilterChanged"
                          materialDesign:HintAssist.Hint="Дата"
                          >
                    <ComboBoxItem Content="Все даты" IsSelected="True"/>
                    <ComboBoxItem Content="За последние 7 дней"/>
                    <ComboBoxItem Content="За месяц"/>
                    <ComboBoxItem Content="За год"/>
                </ComboBox>

                <Button Content="Сбросить" Click="ResetFilters_Click"/>
            </StackPanel>


            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ReviewsItemsControl" Margin="10">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True" Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="320"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Border Width="90"
                                        Height="90"
                                        Margin="0,0,10,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                        <Image Source="{Binding ProductImageUrl}"
                                           Stretch="UniformToFill"
                                           RenderOptions.BitmapScalingMode="HighQuality"
                                           ClipToBounds="True"
                                           Cursor="Hand"
                                           MouseLeftButtonUp="Image_MouseLeftButtonUp"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" Orientation="Vertical" >
                                        <TextBlock Text="{Binding UserName}" 
                                       FontWeight="Bold" 
                                       FontSize="16"/>
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd MMM yyyy}'}" 
                                       FontSize="12"/>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,0">
                                            <TextBlock Text="⭐" FontSize="14" Margin="0,0,3,0"/>
                                            <TextBlock Text="{Binding Rating}" 
                                           FontSize="14"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <ScrollViewer Grid.ColumnSpan="2"
                                              Grid.Row="1"
                                              Height="75"
                                              VerticalScrollBarVisibility="Auto"
                                              CanContentScroll="True"
                                              Padding="0"
                                              Margin="0,5,0,5">
                                        <TextBlock Text="{Binding Text}"
                                               TextWrapping="Wrap"
                                               FontSize="13"
                                               TextTrimming="CharacterEllipsis"/>
                                    </ScrollViewer>

                                    <Grid Grid.ColumnSpan="2"
                                            Grid.Row="2">

                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Button Content="{Binding ReplyButtonText}"
                                            Padding="5,3"
                                            Click="ReplyButton_Click"/>

                                        <Button Content="Удалить отзыв"
                                            Grid.Column="1"
                                            Margin="5,0,0,0"
                                            Padding="5,3"
                                            Click="DeleteButton_Click"/>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- Диалог для ответа -->
        <materialDesign:DialogHost.DialogContent>
            <Border Padding="20" CornerRadius="5" Width="400">
                <StackPanel>
                    <TextBlock Text="Ваш ответ:" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBox x:Name="DialogResponseTextBox" Height="100" TextWrapping="Wrap" AcceptsReturn="True"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Отмена" Click="DialogCancel_Click" Margin="5,0"/>
                        <Button Content="Сохранить" Click="DialogSave_Click" Margin="5,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </materialDesign:DialogHost.DialogContent>

    </materialDesign:DialogHost>
</Page>
